<div class="card text-center">
  <div class="card-header">
    BKS-Tool Version 2.0.1
  </div>
  <div class="card-body">
    <h5 class="card-title">Willkommen zum neuen BKS Anmelde Tool.</h5>
    <% if current_admin %>
      <p class="card-text">Sie sind als Administrator <%= @current_admin.handle %> angemeldet.</p>
      <%= link_to 'Dashboard', dashboard_path, class: "btn btn-lg btn-primary" %>
    <% elsif Presentation.first == nil && Teacher.first == nil && Student.first == nil %>
      <p class="card-text">Diese Seite wurde noch nicht eingerichtet. Bitte kontaktieren Sie das Informatikbüro</p>
      <%= link_to 'Einrichtung', login_path, class: "btn btn-lg btn-primary" %>
    <% else %>
    <p class="card-text">Jetzt Anmelden</p>
      <%= link_to 'Anmelden', login_path, class: "btn btn-lg btn-primary" %>
    <% end %>
  </div>
  <div class="card-footer text-muted">
    Erstellt von Edward Lancaster MA18 <br />
  </div>
</div>
